"use client";

import React from "react";

import { SigninFormHolder } from "./SigninFormHolder";

export const SigninFormContainer = () => {
  return <SigninFormHolder onSubmit={async () => {}} />;
};
